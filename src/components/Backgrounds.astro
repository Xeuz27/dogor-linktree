<div class="absolute inset-0">
    <!-- whole back background -->
    <div class="absolute inset-0 z-10">
        <!-- red left-top side circle -->
        <!-- smaller than 640px -->
        <div id="bg-1" class="absolute inset-0 z-20 sm:hidden"></div>
        <!-- smaller than 768px -->
        <div id="bg-2" class="absolute inset-0 z-20 hidden sm:max-md:block"></div>
        <!-- smaller than 1024px -->
        <div id="bg-3" class="absolute inset-0 z-20 hidden md:max-lg:block"></div>
        <!-- smaller than 1280px -->
        <div id="bg-4" class="absolute inset-0 z-20 hidden lg:max-xl:block"></div>
        <!-- bigger  than 1280px -->
        <div id="bg-5" class="absolute inset-0 z-20 hidden xl:block"></div>

        <!-- black right-bottom side circle -->
        <div id="bg-6" class="absolute inset-0 z-20"></div>
    </div>
</div>
<style>
    #bg-1 {
        background-image: radial-gradient(
            900px at -85% -40%,
            rgba(229, 131, 92, 0.35),
            transparent
        );
    }
    #bg-2 {
        background-image: radial-gradient(
            1100px at -80% -20%,
            rgba(229, 131, 92, 0.35),
            transparent
        );
    }
    #bg-3 {
        background-image: radial-gradient(
            1400px at -80% -50%,
            rgba(229, 131, 92, 0.35),
            transparent
        );
    }
    #bg-4 {
        background-image: radial-gradient(
            1600px at -70% -60%,
            rgba(229, 131, 92, 0.35),
            transparent
        );
    }
    #bg-5 {
        background-image: radial-gradient(
            1600px at -40% -60%,
            rgba(229, 131, 92, 0.35),
            transparent
        );
    }
    #bg-6 {
        background-image: radial-gradient(800px at 80% 90%, rgba(0, 0, 0, 0.75), transparent);
    }
</style>
